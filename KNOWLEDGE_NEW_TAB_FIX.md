# 知识库新标签页打开功能修复总结

## ✅ 已修复的问题

### 1. 新标签页跳转错误

**问题**：
- 点击"新标签页打开"按钮后，跳转到了工作流程看板（或首页）
- 原因是使用了手动拼接的 URL 字符串 `/knowledge/:id`
- 在 Vue Router 应用中，这可能被解析为无效路径或被重定向

**解决方案**：
- ✅ 引入 `useRouter` 钩子
- ✅ 使用 `router.resolve()` 方法解析路由
- ✅ 获取完整的 `href`（包含 base URL 和正确的路径形式）
- ✅ 使用 `window.open(href, '_blank')` 打开

**代码变更**：
```javascript
// 之前（错误）
const route = `/knowledge/${currentKnowledgeArticle.value.id}`
window.open(route, '_blank')

// 现在（正确）
import { useRouter } from 'vue-router'
const router = useRouter()

const { href } = router.resolve({
  name: 'knowledge-article',
  params: { id: articleId }
})
window.open(href, '_blank')
```

### 2. 影响范围
- ✅ **管理员端** (`AdminDashboard.vue`)：预览对话框中的跳转已修复
- ✅ **用户端** (`Knowledge.vue`)：预览对话框中的跳转已同步修复

## 📝 原理解析

`router.resolve` 能够处理：
1. **Base URL**：如果应用部署在子路径下（如 `/app/`），它会自动加上前缀。
2. **Router Mode**：如果是 Hash 模式（`/#/knowledge/1`）或 History 模式，它会自动生成正确的链接格式。
3. **路由参数**：自动填充 `params`，避免手动拼接错误。

现在的跳转链接将是完全正确的，可以直接在浏览器中打开文章详情页。
